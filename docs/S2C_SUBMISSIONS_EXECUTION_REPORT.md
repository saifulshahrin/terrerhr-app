# S2C Submissions Execution Report: `public.submissions`

Branch: `schema-s1-stabilization`
Target disposable project: `terrer-schema-s2c-bootstrap`
Linked ref verified: `epigstfenpqbslgeyrtn`

## Summary

`public.submissions` was reconstructed successfully.

The previous execution had stopped before DDL because prerequisite function `public.update_submission_stage_timestamp()` was missing. That prerequisite was reconstructed, validated, committed, and pushed before this retry.

This fresh execution reconstructed only `public.submissions`.

## Pre-state

Verified before DDL:

- `public.submissions` did not exist
- `public.jobs` existed
- `public.candidates` existed
- `public.companies` existed
- `public.update_updated_at_column()` existed
- `public.update_submission_stage_timestamp()` existed

## Reconstruction

Applied:

- canonical table definition
- owner assignment to `postgres`
- RLS enablement
- constraints:
  - `submissions_candidate_id_fkey`
  - `submissions_company_id_fkey`
  - `submissions_job_id_fkey`
  - `submissions_next_action_check`
  - `submissions_pkey`
  - `submissions_stage_check`
- canonical index:
  - `submissions_job_candidate_unique`
- all 12 canonical policies, including duplicated/overlapping anon/demo policies
- canonical grants for `anon`, `authenticated`, `postgres`, and `service_role`
- triggers:
  - `set_submission_stage_updated_at`
  - `set_updated_at_submissions`

## Validation infrastructure

Created only for validation:

- one temporary `public.job_sources` row
- one temporary `public.companies` row
- one temporary `public.jobs` row
- one temporary `public.candidates` row

Captured IDs:

- `validation_job_source_id = 82736803-8a27-42c1-828b-d9971720354a`
- `validation_company_id = 5`
- `validation_job_id = cf759fcf-44c5-4d64-832a-a0560dd0e012`
- `validation_candidate_id = 81241d7c-60ae-48c7-849c-7f03bd3ee23e`
- `validation_submission_id = 4f6627a4-79e8-4200-9660-f5bb5eb9dd0c`

The candidate ID was generated by the database and reused throughout validation and cleanup.

## Behavior validation

Valid insert:

- inserted one synthetic `public.submissions` row
- `submission_stage = shortlisted`
- `next_action_date` populated
- result: PASS

Duplicate `(job_id, candidate_id)` rejection:

```text
ERROR:  23505: duplicate key value violates unique constraint "submissions_job_candidate_unique"
DETAIL:  Key (job_id, candidate_id)=(cf759fcf-44c5-4d64-832a-a0560dd0e012, 81241d7c-60ae-48c7-849c-7f03bd3ee23e) already exists.
```

Invalid `job_id` FK rejection:

```text
ERROR:  23503: insert or update on table "submissions" violates foreign key constraint "submissions_job_id_fkey"
DETAIL:  Key (job_id)=(ffffffff-ffff-ffff-ffff-ffffffffffff) is not present in table "jobs".
```

Invalid `candidate_id` FK rejection:

```text
ERROR:  23503: insert or update on table "submissions" violates foreign key constraint "submissions_candidate_id_fkey"
DETAIL:  Key (candidate_id)=(ffffffff-ffff-ffff-ffff-ffffffffffff) is not present in table "candidates".
```

Invalid `company_id` FK rejection:

```text
ERROR:  23503: insert or update on table "submissions" violates foreign key constraint "submissions_company_id_fkey"
DETAIL:  Key (company_id)=(999999999999) is not present in table "companies".
```

Invalid `next_action_date` check rejection:

```text
ERROR:  23514: new row for relation "submissions" violates check constraint "submissions_next_action_check"
```

All negative validations were expected successful checks of canonical constraint enforcement.

## Trigger validation

Updated the synthetic submission from:

- `submission_stage = shortlisted`
- `stage_updated_at = 2026-07-04 08:13:05.261885+00`
- `updated_at = 2026-07-04 08:13:05.261885+00`

To:

- `submission_stage = interview`
- `stage_updated_at = 2026-07-04 08:14:39.28466+00`
- `updated_at = 2026-07-04 08:14:39.28466+00`

Confirmed:

- `set_submission_stage_updated_at` fired
- `set_updated_at_submissions` fired
- both timestamps advanced

## Catalog validation

Confirmed:

- `public.submissions` exists
- constraints present:
  - `submissions_candidate_id_fkey`
  - `submissions_company_id_fkey`
  - `submissions_job_id_fkey`
  - `submissions_next_action_check`
  - `submissions_pkey`
  - `submissions_stage_check`
- exactly the canonical indexes:
  - `submissions_pkey`
  - `submissions_job_candidate_unique`
- RLS enabled
- exactly 12 canonical policies present
- exactly 2 triggers present:
  - `set_submission_stage_updated_at`
  - `set_updated_at_submissions`
- canonical grants present, including `MAINTAIN` for:
  - `anon`
  - `authenticated`
  - `postgres`
  - `service_role`

## Cleanup

Removed all synthetic validation rows:

- synthetic `public.submissions` row
- synthetic `public.jobs` row
- synthetic `public.candidates` row
- synthetic `public.companies` row
- synthetic `public.job_sources` row

Post-cleanup verification confirmed:

- `public.submissions` still exists
- synthetic submission no longer exists
- synthetic job no longer exists
- synthetic candidate no longer exists
- synthetic company no longer exists
- synthetic job source no longer exists

## Result

PASS

## Notes

- No migration files were created.
- No production resources were touched.
- No continuation was made into:
  - `activity_log`
  - `applications`
